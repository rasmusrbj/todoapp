# Copy to web/.env.local for local development.

# Where the ConnectRPC backend lives. Server-side only — deliberately not a
# NEXT_PUBLIC_ variable, because the browser never talks to the API directly.
TODOAPP_API_URL=http://127.0.0.1:8081
